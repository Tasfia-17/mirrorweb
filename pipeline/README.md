```
 __  __ ___ ____  ____  ___  ____
|  \/  |_ _|  _ \|  _ \/ _ \|  _ \
| |\/| || || |_) | |_) | | | | |_) |
| |  | || ||  _ <|  _ <| |_| |  _ <
|_|  |_|___|_| \_\_| \_\\___/|_| \_\
```

# MIRROR

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Python 3.11+](https://img.shields.io/badge/python-3.11+-blue.svg)](https://www.python.org/downloads/)


**60 seconds of voice. 5 platform formats. 10 languages. Self-improving.**

MIRROR is a multi-agent AI pipeline that takes a 60-second voice memo and generates
5 platform-optimized videos, then translates them to 10 languages (50 total outputs).
Core pipeline runs in 90 seconds. Full translation queue completes in 8 minutes.
Built for the HeyGen Multi-modal AI Hackathon (May 2026).

---

## What It Does

You speak for 60 seconds. MIRROR produces:

- 5 platform-optimized videos (LinkedIn, TikTok, YouTube, Sales, Podcast)
- 10 language translations of each video via HeyGen Video Translate
- Cinematic B-roll generated by Fal Kling 2.1 Pro
- Hyperframes HTML-to-video compositions with animated overlays
- A live PostHog dashboard tracking every agent decision and API cost
- A self-improving loop: the Optimizer agent reads PostHog data and surfaces
  which format performed weakest so the next run improves it

Total output: 50 pieces of content from one voice memo.
Estimated cost: $4.20 per generation.
Equivalent agency cost: $50,000 per month.

## Core Concepts

### Multi-Agent Orchestration

MIRROR uses 9 specialized agents coordinated by LangGraph:

1. **Intake Agent**: Transcribes audio with ElevenLabs Scribe v2, detects emotion with GPT-4o-mini
2. **Vision Agent**: Analyzes uploaded images for visual context (optional)
3. **Identity Agent**: Clones voice with ElevenLabs IVC, creates Avatar V digital twin
4. **Format Agent**: Rewrites script for 5 platforms in parallel (async optimization)
5. **Critic Agent**: Scores scripts 0-10, triggers rewrite if score < 7 (quality gate)
6. **Cinematic Agent**: Generates 4-shot B-roll with Fal Kling 2.1 Pro
7. **Hyperframes Agent**: Composes HTML overlays (lower thirds, CTAs, waveforms)
8. **Translate Agent**: Translates to 10 languages with HeyGen Video Translate
9. **Optimizer Agent**: Surfaces improvement signals from PostHog analytics

### Conditional Quality Loop

The Critic agent creates a conditional edge in the graph:

```
Format → Critic → [Quality Check]
           ↓           ↓
      if score < 7   if score >= 7
      loop back      proceed to Cinematic
```

This prevents weak scripts from reaching expensive video generation.
Maximum 2 rewrites to avoid infinite loops.

### Self-Improving Pipeline

PostHog tracks every agent decision:
- Quality scores per platform
- Cost per generation
- Latency per stage
- A/B test results for prompt variants

The Optimizer agent reads this data and surfaces which format needs improvement.
In production, it updates PostHog feature flags to improve prompts without code deploys.

### Multi-Modal Input

MIRROR accepts:
- **Audio**: Voice memos (MP3, WAV, M4A)
- **Images**: Reference photos for visual context (JPG, PNG)
- **Text**: Optional script override

The Vision agent analyzes images with GPT-4 Vision and provides scene context
to the Format agent for more relevant scripts.

### Platform-Specific Optimization

Each format has tailored guidelines:

- **LinkedIn**: Professional, value-driven, ends with engagement question
- **TikTok**: Hook in 1 second, fast-paced, vertical 9:16
- **YouTube**: Storytelling arc with intro/body/CTA, landscape 16:9
- **Sales**: Problem-Solution-Proof structure, direct CTA
- **Podcast**: Conversational, audio-first, no visual references

### Hyperframes Composition

Hyperframes renders HTML/CSS/JavaScript to MP4:

- **Podcast**: Animated waveform visualization with Canvas
- **YouTube**: Animated lower thirds with red accent bar, subscribe button
- **LinkedIn**: Professional lower thirds with company branding
- **TikTok**: Flash hooks and trending-style overlays
- **Sales**: Bold headlines and CTA buttons

This enables A/B testing visual elements without re-rendering video.

### Cost Model

Per generation (60s input, 5 formats, 10 languages):

| Service | Cost |
|---|---|
| ElevenLabs Scribe v2 | $0.10 |
| ElevenLabs IVC | $0.10 |
| OpenAI GPT-4o-mini | $0.05 |
| HeyGen Video Agent (x5) | $1.50 |
| HeyGen Video Translate (x10) | $1.50 |
| Fal Kling B-roll | $0.75 |
| Fal FLUX thumbnails | $0.25 |
| **Total** | **$4.25** |

Pricing tiers with 89% margin at Pro level ($199/mo).

### Deployment Options

- **Web**: Next.js 14 app with SSE progress streaming
- **Mobile**: React Native PWA with one-tap voice capture
- **Desktop**: Tauri app with local file processing
- **CLI**: Enhanced with interactive mode and watch mode
- **API**: RESTful API with batch processing and webhooks

---

## Architecture

```
Input: 60-second audio file
        |
        v
[1. Intake Agent]
    ElevenLabs Scribe v2 transcribes audio
    GPT-4o-mini detects emotion and tone
        |
        v
[2. Identity Agent]
    ElevenLabs IVC clones the speaker's voice
    HeyGen Avatar V creates the digital twin
        |
        v
[3. Format Agent]
    GPT-4o-mini rewrites script for 5 platforms
    HeyGen Video Agent generates one video per format
        |
        v
[4. Critic Agent]  <-- conditional loop
    GPT-4o-mini scores each script 0-10
    If any score < 7.0 and rewrite_count < 2:
        loop back to Format Agent
    Else: proceed
        |
        v
[5. Cinematic Agent]
    Fal Kling 2.1 Pro generates 4-shot B-roll narrative
    Fal Wan 2.6 R2V for character-consistent scenes
        |
        v
[6. Hyperframes Agent]
    Renders HTML compositions with animated overlays
    Platform-specific lower thirds, CTAs, waveforms
        |
        v
[7. Translate Agent]
    HeyGen Video Translate: 10 languages
    Spanish, French, German, Japanese, Chinese,
    Portuguese, Korean, Arabic, Hindi, Italian
        |
        v
[8. Optimizer Agent]
    Reads PostHog quality scores
    Surfaces weakest format for next run
        |
        v
Output: 50 pieces of content
```

The graph is built with LangGraph. The Critic node uses conditional edges to
loop back to Format if quality is below threshold. This is what makes MIRROR
an agent system rather than a sequential pipeline.

---

## Sponsor Stack

### HeyGen (Core Video Engine)
- Avatar V: digital twin creation from a 15-second video clip
- Video Agent: prompt-to-video with automatic scene composition
- Video Translate: lip-synced dubbing in 40+ languages
- Streaming Avatar: real-time LiveAvatar for interactive demo

### ElevenLabs (Voice Layer)
- Scribe v2: transcription with emotion and language detection
- Instant Voice Clone (IVC): voice clone from the same 60-second input
- Multilingual TTS: cloned voice speaks all 10 languages

### Fal.ai (Visual Assets)
- Kling 2.1 Pro: 4-shot cinematic B-roll with explicit camera direction
- Wan 2.6 R2V: character-consistent scenes from reference image
- FLUX Schnell: platform-optimized thumbnail generation

### PostHog (Observability and Learning)
- LLM tracing: every API call tracked as $ai_generation event
- Pipeline funnel: Intake to Output with per-step latency
- Feature flags: A/B test prompt variants at runtime without code deploy
- Optimizer loop: quality scores feed back into the next generation

---

## Project Structure

```
mirror/
  mirror.py               CLI entry point
  config.py               Central config loader with _require() helper
  requirements.txt        Pinned dependencies

  agents/
    intake.py             Transcription and emotion detection
    identity.py           Voice clone and Avatar V creation
    format.py             Script rewriting for 5 platforms
    critic.py             Quality gate (scores scripts 0-10)
    cinematic.py          B-roll generation via Fal
    hyperframes.py        HTML-to-video composition
    translate.py          10-language translation
    optimizer.py          PostHog-driven improvement signals
    liveavatar.py         Real-time interactive avatar session

  core/
    orchestrator.py       LangGraph state machine with conditional edges
    state.py              MirrorState TypedDict
    prompts.py            All LLM prompt templates
    retry.py              Exponential backoff decorator
    cost.py               Cost calculator and margin analysis

  clients/
    elevenlabs.py         ElevenLabs API wrapper
    heygen.py             HeyGen API wrapper with polling
    fal.py                Fal.ai API wrapper
    posthog_client.py     PostHog event tracking and prompt management

  api/
    app.py                FastAPI app with /generate endpoint

  static/
    index.html            Demo UI
    templates/
      podcast.html        Hyperframes podcast waveform template
      youtube.html        Hyperframes YouTube lower thirds template
```

---

## Setup

### Prerequisites

- Python 3.11+
- API keys for HeyGen, ElevenLabs, Fal.ai, PostHog, and OpenAI

### Install

```bash
git clone https://github.com/Tasfia-17/mirror.git
cd mirror
pip install -r requirements.txt
cp .env.example .env
# Edit .env and add your API keys
```

### Environment Variables

```
ELEVENLABS_API_KEY=your_elevenlabs_key
HEYGEN_API_KEY=your_heygen_key
FAL_KEY=your_fal_key
POSTHOG_API_KEY=your_posthog_key
POSTHOG_HOST=https://app.posthog.com
OPENAI_API_KEY=your_openai_key
```

---

## Usage

### CLI

```bash
python mirror.py your_voice_memo.mp3
```

Output:

```
MIRROR -- Processing your_voice_memo.mp3
============================================================

Pipeline complete!
Duration: 18.9s
Transcript: Your ideas deserve to be heard everywhere...
Emotion: confident
Videos generated: 5
Translations: 10
Total outputs: 15
Quality scores: {'linkedin': 8.5, 'tiktok': 7.9, 'youtube': 8.2, 'sales': 8.7, 'podcast': 7.6}
```

### Web API

```bash
uvicorn api.app:app --reload
```

Then open http://localhost:8000/static/index.html or POST to /generate:

```bash
curl -X POST http://localhost:8000/generate \
  -F "file=@voice_memo.mp3"
# Returns: {"job_id": "abc12345", "status": "running"}

curl http://localhost:8000/jobs/abc12345
# Returns: {"status": "complete", "result": {...}}
```

---

## The Critic Loop

The most important architectural feature is the conditional quality gate.

After the Format Agent generates 5 scripts, the Critic Agent scores each one
on hook strength, CTA clarity, emotional resonance, and platform fit (0-10).

If any score is below 7.0 and the rewrite count is below 2, the graph routes
back to the Format Agent with the scores in state. The Format Agent sees the
rewrite_count and can adjust its approach.

This prevents weak scripts from reaching expensive video generation.

In the demo, you will see:

```
[Format] LinkedIn script generated
[Critic] LinkedIn score: 6.2/10 - hook too weak
[Format] Rewriting LinkedIn (attempt 2)...
[Critic] LinkedIn score: 8.5/10 - approved
[Cinematic] Proceeding to video generation
```

---

## PostHog Integration

Every agent step emits a PostHog event. The live dashboard shows:

1. Agent Trace Funnel: Intake to Output with per-step latency
2. Quality Scores: bar chart of 5 platform scores per run
3. Cost Breakdown: HeyGen, ElevenLabs, Fal, OpenAI costs per generation
4. A/B Test: two prompt variants for LinkedIn hook style

The Optimizer Agent reads the quality_scores from state and surfaces the
weakest format. In a production deployment, it would query PostHog HogQL
to compare scores across runs and update the feature flag prompt variant.

---

## Cost Model

Per generation (60-second input, 5 formats, 10 languages):

| Service | Cost |
|---|---|
| ElevenLabs Scribe v2 | $0.10 |
| ElevenLabs IVC | $0.10 |
| OpenAI GPT-4o-mini (6 calls) | $0.05 |
| HeyGen Video Agent (x5) | $1.50 |
| HeyGen Video Translate (x10) | $1.50 |
| Fal Kling B-roll | $0.75 |
| Fal FLUX thumbnails | $0.25 |
| Total | $4.25 |

Pricing tiers:

| Tier | Price | Margin |
|---|---|---|
| Starter | $49/mo | 92.8% |
| Pro | $199/mo | 89.4% |
| Enterprise | $499/mo | 82.4% |

Breakeven: 23 Pro customers.

---

## Hyperframes Templates

MIRROR uses Hyperframes to compose final videos with HTML-based overlays.

The podcast template renders an animated waveform visualization using Canvas.
The YouTube template adds animated lower thirds with a red accent bar and a
subscribe button that appears in the final 5 seconds.

To render a template locally:

```bash
npx hyperframes render --input static/templates/podcast.html --output podcast_final.mp4
```

---

## LiveAvatar Demo

After the pipeline completes, MIRROR can open a real-time LiveAvatar session
so the judge can talk to their own digital twin.

```python
from agents.liveavatar import create_session, answer_question

session = create_session(final_state)
print(session["room_url"])  # Open in browser

answer = answer_question("What did I say about AI?", final_state)
print(answer)
```

The avatar responds using the pipeline context: original transcript, detected
emotion, formats generated, and quality scores.

---

## Built With

- HeyGen API (Avatar V, Video Agent, Video Translate, LiveAvatar)
- ElevenLabs (Scribe v2, IVC, Multilingual TTS)
- Fal.ai (Kling 2.1 Pro, Wan 2.6 R2V, FLUX Schnell)
- PostHog (LLM observability, feature flags, funnel analytics)
- LangGraph (state machine with conditional edges)
- OpenAI GPT-4o-mini (emotion detection, script rewriting, critic scoring)
- FastAPI (web API)
- Hyperframes (HTML-to-video composition)

---

## Hackathon

Built at the HeyGen Multi-modal AI Hackathon, May 14-15, 2026.
180 Sansome St, San Francisco + Virtual.

The philosophical question MIRROR asks:

> Why should a human have to be present to communicate their ideas?

---

## License

MIT


## Hackathon Demo

This project was built for the HeyGen Multi-modal AI Hackathon (May 14-15, 2026).

**Demo video**: Coming soon
**Slides**: Coming soon

## Acknowledgments

Built with HeyGen, ElevenLabs, Fal.ai, and PostHog APIs.
Special thanks to the hackathon organizers and sponsors.
